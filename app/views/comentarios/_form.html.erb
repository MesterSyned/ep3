<%= simple_form_for([@topico, @topico.comentarios.build], remote: true) do |f| %>

<div class="field">
 <div class="control">
    <%= f.input :comentario, input_html: {class: 'textarea' }, wrapper: false, label_html: { class: 'label' } %>
 </div>
</div>

<%= f.button :submit, 'Deixe um comentário', class:'button is-info' %>

<% end %>